services:
  backend:
    platform: linux/amd64
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: backend
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Database removed per user request. Add env vars back if needed.
 
